<html>
<head>
<title>Editing Book</title>
</head>
    <body>
        <h1>Update the {{ book.getTitle }} task </h1>

        <form action="/books/{{ book.getBookId }}" method="post">
          <input name="_method" type="hidden" value="patch">

          <label for="description">Rename your book:</label>
          <input id="title" name="title" type="text">

          <button type="submit">Update</button>
        </form>

        <form action="/books/{{ book.getBookId }}" method="post">
          <input name="_method" type="hidden" value="delete">

          <button type="submit">Delete this book</button>
        </form>

        <p><a href='/'>Home</a></p>
    </body>
</html>
